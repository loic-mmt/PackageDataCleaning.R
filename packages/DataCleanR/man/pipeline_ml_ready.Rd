% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pipeline_ml_ready}
\alias{pipeline_ml_ready}
\title{ML-ready pipeline}
\usage{
pipeline_ml_ready(
  data,
  required_columns = NULL,
  strict = TRUE,
  normalize = TRUE,
  do_currency = TRUE,
  finalize = FALSE,
  dedup_keys = NULL,
  dedup_keep = c("first", "last"),
  cap_col = "salary_in_usd",
  cap_lower = 0.01,
  cap_upper = 0.99,
  cap_side = c("both", "upper", "lower"),
  cap_verbose = FALSE,
  check_ranges = TRUE,
  ranges_min_year = 2000,
  ranges_max_year = as.integer(format(Sys.Date(), "\%Y")),
  impute_verbose = FALSE,
  cat_constant = "NA"
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{required_columns}{Optional character vector of required columns.}

\item{strict}{Stop on missing required columns when \code{TRUE} (default).}

\item{normalize}{Logical. Apply \code{normalize_all} when all expected columns are present.}

\item{do_currency}{Logical. Apply \code{convert_currency_to_usd} (default \code{TRUE}).}

\item{dedup_keys, dedup_keep, cap_col, cap_lower, cap_upper, cap_side, cap_verbose}{See \code{\link[=pipeline_strict_clean]{pipeline_strict_clean()}}.}

\item{check_ranges, ranges_min_year, ranges_max_year}{See \code{\link[=pipeline_strict_clean]{pipeline_strict_clean()}}.}

\item{impute_verbose, cat_constant}{See \code{\link[=pipeline_strict_clean]{pipeline_strict_clean()}}.}
}
\value{
A fully cleaned \code{data.frame} ready for modelling.
}
\description{
Strict pipeline + business normalisations + optional currency conversion.
}
