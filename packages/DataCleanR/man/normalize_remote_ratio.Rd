% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalisation.R
\name{normalize_remote_ratio}
\alias{normalize_remote_ratio}
\title{Normalise the \code{remote_ratio} column}
\usage{
normalize_remote_ratio(data, binary = FALSE, threshold = 50)
}
\arguments{
\item{data}{The data.frame containing the \code{remote_ratio} column.}

\item{binary}{Logical (default \code{FALSE}). Activates binary mode (only 0 or 100).}

\item{threshold}{Numeric (default 50). Threshold for binarisation.
\itemize{
\item If value >= \code{threshold}: becomes 100.
\item If value < \code{threshold}: becomes 0.
}}
}
\value{
The \code{data.frame} with the corrected \code{remote_ratio} column.
}
\description{
Clean and standardise the \code{remote_ratio} column of a data.frame.
Ensures that values are numeric and between 0 and 100.
}
\details{
The processing includes:
\itemize{
\item Conversion to numeric (conversion errors become \code{NA}).
\item Bounding values (\code{pmin}/\code{pmax}): anything < 0 becomes 0, anything > 100 becomes 100.
\item (Optional) Binarisation: If \code{binary = TRUE}, values become either 0 or 100
according to the \code{threshold}.
}
}
\examples{
df <- data.frame(remote_ratio = c("0", "100", "50", "30", "-5", "abc"))

# Standard normalisation (numeric + bounding 0-100)
normalize_remote_ratio(df)

# Binary normalisation (everything >= 50 becomes 100, the rest 0)
normalize_remote_ratio(df, binary = TRUE, threshold = 50)
}
\seealso{
Other Normalization functions: 
\code{\link{normalize_all}()},
\code{\link{normalize_factor}()}
}
\concept{Normalization functions}
