% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pipeline_strict_clean}
\alias{pipeline_strict_clean}
\title{Strict cleaning pipeline}
\usage{
pipeline_strict_clean(
  data,
  required_columns = NULL,
  strict = TRUE,
  dedup_keys = NULL,
  dedup_keep = c("first", "last"),
  cap_col = "salary_in_usd",
  cap_lower = 0.01,
  cap_upper = 0.99,
  cap_side = c("both", "upper", "lower"),
  cap_verbose = FALSE,
  check_ranges = TRUE,
  ranges_min_year = 2000,
  ranges_max_year = as.integer(format(Sys.Date(), "\%Y")),
  impute_verbose = FALSE,
  cat_constant = "NA"
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{required_columns}{Optional character vector of required columns.}

\item{strict}{Stop on missing required columns when \code{TRUE} (default).}

\item{dedup_keys}{Columns used for deduplication; \code{NULL} uses all columns.}

\item{dedup_keep}{\code{"first"} (default) or \code{"last"} row to keep when duplicates
are detected.}

\item{cap_col}{Column to winsorise (default \code{"salary_in_usd"}). If the column
is missing or not numeric the step is skipped with a warning.}

\item{cap_lower}{Lower quantile for winsorisation (default 0.01).}

\item{cap_upper}{Upper quantile for winsorisation (default 0.99).}

\item{cap_side}{Side to cap: \code{"both"} (default), \code{"upper"} or \code{"lower"}.}

\item{cap_verbose}{Logical, forwarded to \code{cap_outliers_salary}.}

\item{check_ranges}{Logical. When \code{TRUE} (default) remove rows failing
\code{validate_ranges} if the needed columns are present.}

\item{ranges_min_year, ranges_max_year}{Bounds passed to \code{validate_ranges}.}

\item{impute_verbose}{Logical, forwarded to \code{impute_missing}.}

\item{cat_constant}{Value used when imputing categorical columns with the
\code{"new_level"} strategy.}
}
\value{
A cleaned \code{data.frame}.
}
\description{
Minimal pipeline + aggressive deduplication + winsorisation of salary +
strict imputation. Optionally removes rows outside valid ranges.
}
