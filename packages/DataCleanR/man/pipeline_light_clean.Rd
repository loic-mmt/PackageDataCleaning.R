% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pipeline_light_clean}
\alias{pipeline_light_clean}
\title{Light cleaning pipeline (EDA)}
\usage{
pipeline_light_clean(
  data,
  required_columns = NULL,
  strict = TRUE,
  dedup_keys = NULL,
  dedup_keep = c("first", "last"),
  num_method = "median",
  cat_method = "mode",
  num_constant = 0,
  cat_constant = "Missing",
  impute_verbose = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{required_columns}{Optional character vector of required columns.}

\item{strict}{Stop on missing required columns when \code{TRUE} (default).}

\item{dedup_keys}{Character vector of columns used to detect duplicates
(after standardisation). If \code{NULL}, all columns are used.}

\item{dedup_keep}{Which duplicate to keep: \code{"first"} (default) or \code{"last"}.}

\item{num_method}{Imputation method for numeric columns (\code{"median"}, \code{"mean"},
\code{"constant"}).}

\item{cat_method}{Imputation method for categorical columns (\code{"mode"},
\code{"constant"}, \code{"new_level"}).}

\item{num_constant}{Value used when \code{num_method = "constant"}.}

\item{cat_constant}{Value used when \code{cat_method} is \code{"constant"} or \code{"new_level"}.}

\item{impute_verbose}{Logical, passed to \code{impute_missing}.}
}
\value{
A deduplicated and lightly imputed \code{data.frame}.
}
\description{
Minimal pipeline + deduplication + soft imputation.
}
