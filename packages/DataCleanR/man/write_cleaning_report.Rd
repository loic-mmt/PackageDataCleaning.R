% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{write_cleaning_report}
\alias{write_cleaning_report}
\title{Write a cleaning report to a text file}
\usage{
write_cleaning_report(
  data,
  original_data = NULL,
  file = "cleaning_report.txt",
  stats_list = NULL
)
}
\arguments{
\item{data}{The cleaned (final) \code{data.frame}.}

\item{original_data}{(Optional) The raw \code{data.frame} (before cleaning) to
compare removed rows.}

\item{file}{Path to the output file (e.g. \code{"reports/cleaning_log.txt"}).}

\item{stats_list}{A named list of counters or messages
(e.g. \code{list(outliers_capped = 12, rows_removed = 5)}).}
}
\value{
Invisibly returns the path to the created file.
}
\description{
Create a text file summarising the state of the data set after cleaning.
It optionally compares the dimensions before/after and records the provided statistics.
}
\details{
The report contains:
\itemize{
\item The date and time of the report.
\item The dimensions of the final data set.
\item The number of remaining missing values (NA) per column.
\item A section of operational logs displaying the counters provided in \code{stats_list}.
}
}
\examples{
# Raw data
df_raw <- data.frame(id = 1:5, val = c(10, NA, 30, 1000, 50))

# Cleaned data (example)
df_clean <- data.frame(id = c(1, 3, 4, 5), val = c(10, 30, 100, 50))

# List of operations performed (collected during the script)
my_stats <- list(
  "NA imputed" = 1,
  "Outliers capped" = 1,
  "Rows removed" = 1
)

# Generate the report
tmp_file <- tempfile(fileext = ".txt")
write_cleaning_report(df_clean, original_data = df_raw, file = tmp_file, stats_list = my_stats)

# Read the result
cat(readLines(tmp_file), sep = "\n")
}
\seealso{
Other Export functions: 
\code{\link{export_csv}()}
}
\concept{Export functions}
