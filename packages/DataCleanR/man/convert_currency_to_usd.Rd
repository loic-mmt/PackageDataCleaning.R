% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currencies.R
\name{convert_currency_to_usd}
\alias{convert_currency_to_usd}
\title{Convert salaries to USD}
\usage{
convert_currency_to_usd(data)
}
\arguments{
\item{data}{A \code{data.frame} that must contain at least the following columns:
\itemize{
\item \code{salary}: The gross amount in the local currency.
\item \code{salary_currency}: The ISO code of the currency (e.g. "EUR", "GBP").
\item \code{work_year}: The reference fiscal year.
\item \code{salary_in_usd}: (Optional, will be overwritten/created).
}}
}
\value{
The \code{data.frame} enriched with the computed \code{salary_in_usd} column.
}
\description{
Convert local salary amounts to US dollars (USD).
This function takes into account the variation of exchange rates by work year (\code{work_year}).
}
\details{
The conversion procedure is as follows:
\enumerate{
\item \strong{Validation}: Checks that the columns \code{salary}, \code{salary_currency} and \code{work_year} are present.
\item \strong{Typing}: Ensures that the salary is numeric and the year is an integer.
\item \strong{Join}: Merges the data with the reference table \code{exchange_rates_to_usd} based on currency and year.
\item \strong{Computation}: Applies the exchange rate to update \code{salary_in_usd}.
\item \strong{Cleanup}: Removes temporary rate columns (\code{rate}).
}
}
\note{
This function depends on the global object \code{exchange_rates_to_usd}, which must contain the columns \code{currency}, \code{year} and \code{rate}.
}
\examples{
# For the example to work, we simulate the exchange rate table
# Normally, this object 'exchange_rates_to_usd' is loaded in the package
exchange_rates_to_usd <- data.frame(
  currency = c("EUR", "GBP"),
  year = c(2023, 2023),
  rate = c(1.1, 1.25) # 1 EUR = 1.1 USD
)

df <-  data.frame(
  salary = 60000,
  salary_currency = "GBP",
  salary_in_usd = 75000,
  work_year = 2023
)

# Conversion
convert_currency_to_usd(df)
}
\concept{Currency functions}
