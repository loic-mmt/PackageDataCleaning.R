% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{pipeline_no_impute}
\alias{pipeline_no_impute}
\title{Pipeline without imputation}
\usage{
pipeline_no_impute(
  data,
  required_columns = NULL,
  strict = TRUE,
  dedup_keys = NULL,
  dedup_keep = c("first", "last")
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{required_columns}{Optional character vector of required columns.}

\item{strict}{Stop on missing required columns when \code{TRUE} (default).}

\item{dedup_keys}{Columns used for deduplication; \code{NULL} uses all columns.}

\item{dedup_keep}{\code{"first"} (default) or \code{"last"} row to keep when duplicates
are detected.}
}
\value{
A deduplicated \code{data.frame} with missing values unchanged.
}
\description{
Minimal pipeline + deduplication. Missing values are preserved for downstream steps.
}
